---
title: "Quickstart"
description: "Clone, configure, and run Stoneturner locally. No remote database required — Stoneturner runs against a local Turso/libSQL file out of the box."
sidebarTitle: "Quickstart"
---

This guide takes you from zero to a running Stoneturner server with hot reload on port 9000.

## Prerequisites

Before you begin, you must have:

- [Bun](https://bun.com) v1.3+
- A [Vercel AI Gateway API key](https://vercel.com/ai-gateway) (used for embeddings)

## Get started

<Steps>
  <Step title="Install">
    Clone the repository and install dependencies.

    ```bash
    git clone https://github.com/jackmuva/stoneturner.git
    cd stoneturner
    bun install
    ```
  </Step>
  <Step title="Configure environment">
    Copy the example environment file and fill in your values.

    ```bash
    cp .env.example .env
    ```

    | Variable | Required | Description |
    |---|---|---|
    | `BUN_PUBLIC_BACKEND_BASE_URL` | Yes | Backend URL, inlined at build time (e.g. `http://localhost:9000`) |
    | `FRONTEND_BASE_URL` | Yes | Frontend URL, used for CORS (e.g. `http://localhost:9000`) |
    | `AI_GATEWAY_API_KEY` | Yes | API key for the AI gateway (embeddings) |
    | `BUN_PUBLIC_DEV_MODE` | No | Set to `true` to create a test database for developing new integrations (default: `false`) |
  </Step>
  <Step title="Set up the database">
    Generate and apply migrations. This creates a local `stoneturner.db` Turso/libSQL file — no remote database URL is needed for local development.

    ```bash
    bun run generate
    bun run migrate
    ```
  </Step>
  <Step title="Run the dev server">
    Start the server with hot reload on port 9000.

    ```bash
    bun dev
    ```
  </Step>
</Steps>

## Build for production

```bash
bun run build
bun start
```

<Note>
  In dev mode (`BUN_PUBLIC_DEV_MODE=true`), a separate test database is created so you can iterate on new integrations without affecting your primary data.
</Note>

## Next steps

<Columns cols={2}>
  <Card title="How Stoneturner Works" icon="arrows-rotate" href="/how-stoneturner-works">
    Understand the connect → sync → search pipeline.
  </Card>
  <Card title="Add an Integration" icon="puzzle-piece" href="/new-integrations">
    Build a new data source under `src/integrations/`.
  </Card>
</Columns>
